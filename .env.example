# Copy this file to .env before running Docker locally.
#
# Frontend port exposed on the host.
VITE_PORT=4200

# Frontend port inside the app container.
APP_PORT=4200

# API port exposed on the host and used inside the api container.
API_PORT=3001
