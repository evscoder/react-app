services:
  app:
    container_name: my-app
    build:
      context: .
      dockerfile: Dockerfile
      args:
        APP_PORT: ${APP_PORT:-4200}
    ports:
      - "${VITE_PORT:-4200}:${APP_PORT:-4200}"
    volumes:
      - ./app:/app
      - /app/node_modules
    env_file:
      - .env
